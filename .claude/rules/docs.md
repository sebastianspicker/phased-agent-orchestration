---
paths:
  - "docs/**"
  - "AGENTS.md"
  - "README.md"
---

# Documentation

- Markdown links are validated by `scripts/check-markdown-links.py` as part of `./scripts/verify.sh`.
- Ensure all relative links resolve to existing files.
- Do not introduce stale references to deleted files.
- Repo map: `docs/REPO_MAP.md`. Runbook: `docs/RUNBOOK.md`.
