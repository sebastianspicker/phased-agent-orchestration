Initialize a new pipeline run directory under `.pipeline/`.

```bash
./scripts/pipeline-init.sh
```

This creates the pipeline state file and stage directories. Run this before starting any orchestration pipeline.
