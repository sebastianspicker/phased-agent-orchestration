# Codex Adapter Entry

Use this repository with the Codex adapter pipeline:

1. Read `AGENTS.md` for repository rules and verification expectations.
2. Use `adapters/codex/skills/orchestration-pipeline/SKILL.md` as the orchestration entrypoint.
3. Run `./scripts/verify.sh` before proposing completion.
