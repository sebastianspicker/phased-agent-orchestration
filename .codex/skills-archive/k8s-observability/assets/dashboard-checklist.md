# Dashboard checklist (template)

- Golden signals visible (traffic/errors/latency/saturation).
- Rollout/version markers visible.
- Per-dependency signals available (DB, cache, queue).
- Pod-level signals (restarts, readiness, CPU/mem) visible.
- Links to runbooks/playbooks exist.

