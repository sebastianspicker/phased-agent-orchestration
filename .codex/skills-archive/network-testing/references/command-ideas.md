# Tool-agnostic command ideas (examples)

Pick tools that are allowed in your environment.

- DNS: `dig`, `nslookup`
- Reachability: `ping`, `nc`, `curl`
- Path: `traceroute`, `mtr`
- Throughput: `iperf3` (schedule; can be disruptive)
- MTU: large `ping` with DF (platform-specific flags)

