#cloud-config

users:
  - name: user
    groups: [sudo]
    shell: /bin/bash
    ssh_authorized_keys:
      - "ssh-ed25519 AAAA... replace-me"

package_update: true
packages:
  - curl
  - ca-certificates

runcmd:
  - [ sh, -lc, "echo 'cloud-init complete'" ]

