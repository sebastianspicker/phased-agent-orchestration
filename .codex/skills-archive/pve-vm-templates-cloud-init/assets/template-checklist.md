# VM template checklist (template)

- Base OS version recorded.
- Baseline packages installed (and list documented).
- Users/SSH baseline set (no password login if not required).
- Updates configured (manual or unattended policy documented).
- Cloud-init installed and tested (if used).
- Template version tag recorded.
- Test VM provisioned and verified.

