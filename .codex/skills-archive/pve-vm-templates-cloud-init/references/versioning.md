# Template versioning notes

- Treat templates like releases: change log + version number.
- Prefer immutable template versions; don’t mutate “latest” silently.
- Keep a rollback path: previous template version remains available until all consumers migrate.

