# Permissions checklist (starter)

- Service user is non-root and least-privileged.
- Config files readable only by intended users/groups.
- Data directories have correct ownership and mode.
- Logs do not contain secrets; log rotation is configured.

