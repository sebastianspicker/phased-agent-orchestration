# Gemini Adapter Entry

Use this repository with the Gemini adapter pipeline:

1. Read `AGENTS.md` for repository rules and verification expectations.
2. Use `adapters/gemini/skills/orchestration-pipeline/SKILL.md` as the orchestration entrypoint.
3. Run `./scripts/verify.sh` before proposing completion.
