# Cursor Adapter Entry

Use this repository with the Cursor adapter pipeline:

1. Read `AGENTS.md` for repository rules and verification expectations.
2. Use `adapters/cursor/skills/orchestration-pipeline/SKILL.md` as the orchestration entrypoint.
3. Run `./scripts/verify.sh` before proposing completion.
